<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <style>
    /* General Reset */
    body, h1, p, form, input, button {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* Body Styling */
    body {
      font-family: Arial, sans-serif;
      display: flex;
      height: 100vh;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #8BBF7B, #F7A5A0); /* Lighter gradient */
      flex-direction: column;
      overflow: hidden;
    }

    /* Header Styling */
    header {
      width: 100%;
      background-color: #3b7a57;
      color: white;
      padding: 20px;
      text-align: center;
      font-size: 24px;
      font-weight: bold;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      margin-bottom: 20px;
    }

    /* Book-style layout */
    .container {
      display: flex;
      max-width: 1200px;
      width: 100%;
      height: 70%;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
      position: relative;
    }

    /* Left Side (Login Form) */
    .login-container {
      flex: 1;
      background-color: white;
      padding: 40px;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      border-radius: 8px 0 0 8px;
      box-shadow: 2px 0 15px rgba(0, 0, 0, 0.1);
      position: relative;
    }

    /* Back Button in Top Left */
    .back-button {
      position: absolute;
      top: 20px;
      left: 20px;
      background: none;
      border: none;
      color: #3b7a57;
      font-size: 18px;
      cursor: pointer;
      display: flex;
      align-items: center;
    }

    .back-button i {
      margin-right: 5px;
    }

    h1 {
      color: #3b7a57;
      margin-bottom: 20px;
      font-size: 28px;
      font-weight: bold;
    }

    /* Form Styling */
    label {
      display: block;
      margin-bottom: 8px;
      font-weight: 600;
      color: #333;
      width: 100%;
    }

    .input-container {
      position: relative;
      width: 100%;
      margin-bottom: 20px;
    }

    input {
      width: 100%;
      padding: 12px 40px 12px 40px;
      margin: 10px 0;
      border: 1px solid #ddd;
      border-radius: 4px;
      font-size: 16px;
      transition: border-color 0.3s, box-shadow 0.3s;
    }

    input:focus {
      border-color: #3b7a57;
      box-shadow: 0 0 5px rgba(59, 122, 87, 0.5);
      outline: none;
    }

    input::placeholder {
      color: #aaa;
    }

    .input-container i {
      position: absolute;
      top: 50%;
      left: 15px;
      transform: translateY(-50%);
      color: #3b7a57;
    }

    button {
      width: 100%;
      padding: 14px;
      background-color: #3b7a57;
      color: #fff;
      font-size: 16px;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.3s;
    }

    button:focus {
      outline: none;
      transform: translateY(-2px);
    }

    button:hover {
      background-color: #2c5a3b;
    }

    /* Right Side (Gradient Background) */
    .gradient-bg {
      flex: 1;
      background: linear-gradient(135deg, #8BBF7B, #F7A5A0);
      border-radius: 0 8px 8px 0;
      animation: gradientAnimation 5s ease infinite;
    }

    /* Gradient Animation */
    @keyframes gradientAnimation {
      0% {
        background: linear-gradient(135deg, #8BBF7B, #F7A5A0);
      }
      50% {
        background: linear-gradient(135deg, #F7A5A0, #8BBF7B);
      }
      100% {
        background: linear-gradient(135deg, #8BBF7B, #F7A5A0);
      }
    }

    /* Error Message */
    p {
      color: red;
      font-size: 14px;
      margin-top: 10px;
      font-weight: bold;
    }

    /* Responsive Design */
    @media (max-width: 768px) {
      .container {
        flex-direction: column;
      }
      .login-container {
        border-radius: 8px;
        box-shadow: none;
      }
      .gradient-bg {
        border-radius: 8px;
        margin-top: 20px;
      }
    }
  </style>
</head>
<body>

  <!-- Header Section -->
  <header>
    WorkSpy - Secure Login
  </header>

  <!-- Main Content Section -->
  <div class="container">

    <!-- Left Side: Login Form -->
    <div class="login-container">
      <!-- Back Button -->
      <button class="back-button" onclick="window.location.href='/'">
        <i class="fas fa-arrow-left"></i> Back
      </button>
      
      <h1>Login</h1>
      <form action="/login" method="POST">
        
        <!-- Email Input -->
        <div class="input-container">
          <i class="fas fa-envelope"></i>
          <input type="email" name="email" id="email" required placeholder="Enter your email">
        </div>

        <!-- Password Input -->
        <div class="input-container">
          <i class="fas fa-lock"></i>
          <input type="password" name="password" id="password" required placeholder="Enter your password">
        </div>

        <button type="submit">Login</button>
        
      </form>

      <% if (error) { %>
        <p><%= error %></p>
      <% } %>
    </div>

    <!-- Right Side: Gradient Background -->
    <div class="gradient-bg"></div>
  </div>

</body>
</html>
