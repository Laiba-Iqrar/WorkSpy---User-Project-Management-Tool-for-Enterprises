<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Apply for Leave</title>
  <style>
    /* General Styles */
    body {
      background-color: #F4F8FB; /* Light background */
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
    }

     /* Header Styling */
     .header {
      background: #3B7A57; /* Dark Green */
      color: #FFF;
      padding: 10px 0;
      text-align: center;
      position: fixed;
      top: 0;
      width: 100%;
      z-index: 1000;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .header-title {
      font-size: 28px;
      margin: 0;
      font-weight: bold;
    }

    .slogan {
      font-size: 14px;
      margin-top: 2px;
      font-style: italic;
      color:#FFDAB9;
    }

    .nav-menu {
      display: flex;
      justify-content: center;
      gap: 20px;
      margin-top: 10px;
    }

    .nav-menu a {
      text-decoration: none;
      color: #FFF;
      font-weight: normal;
      padding: 10px 15px;
      border-radius: 6px;
      transition: background-color 0.3s, color 0.3s;
    }

    .nav-menu a:hover,
    .nav-menu a.active {
      background-color: gray; /* Coral */
      color: #000;
    }


    /* Main Content Section */
    .content {
      margin-top: 120px;
      display: flex;
      justify-content: center;
      padding: 20px;
    }

    /* Form Container Styling */
    .form-container {
      background: #FFF;
      width: 70%;
      max-width: 900px;
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .form-header {
      text-align: center;
      border-bottom: 2px solid #F26D5B; /* Coral */
      padding-bottom: 10px;
    }

    .form-header h3 {
      font-size: 24px;
      color:  black; /* Dark Green */
      margin: 0;
    }

    .form-header p {
      font-size: 14px;
      color: #666;
      margin-top: 5px;
    }

    .form-body {
      display: flex;
      flex-direction: column;
      gap: 15px;
    }

    /* Form Elements */
    label {
      font-weight: bold;
      color: #3B7A57; /* Dark Green */
    }

    input, select, textarea {
      width: 100%;
      padding: 12px;
      border: 1px solid #CCC;
      border-radius: 8px;
      font-size: 14px;
    }

    input:focus, select:focus, textarea:focus {
      border-color: #3B7A57; /* Green focus */
      outline: none;
    }

    /* Buttons Styling */
    .button-group {
      display: flex;
      justify-content: center; /* Centering the buttons */
      gap: 15px; /* Spacing between buttons */
      margin-top: 15px;
    }

    button {
      padding: 12px;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: all 0.3s;
    }

    button[type="submit"] {
      background-color: #004d40; /* Dark Green */
      color: #FFF;
    }

    button[type="submit"]:hover {
      background-color: #285943; /* Darker Green */
    }

    button[type="button"] {
      background-color: #F26D5B; /* Coral */
      color: #FFF;
    }

    button[type="button"]:hover {
      background-color: #C55647; /* Darker Coral */
    }

    /* Footer Styling */
    .footer {
      text-align: center;
      padding: 10px;
      background: #3B7A57; /* Dark Green */
      color: #FFF;
      font-size: 12px;
    }

  </style>
</head>
<body>
  <!-- Header -->
  <header class="header">
    <div class="header-title">WORKSPY</div>
    <div class="slogan">"Empowering Workforces, Simplifying Lives"</div>
    <div class="nav-menu">
      <a href="/timesheets">Timesheets</a>
      <a href="/leave/apply-leave" class="active">Apply for Leave</a>
      <a href="/leave" >Leave List</a>
      <a href="/reports/task-reports">View Task Reports</a>
    </div>
  </header>

  <!-- Content -->
  <div class="content">
    <div class="form-container">
      <div class="form-header">
        <h3>Leave</h3>
        <p>Submit your leave application with complete details for prompt approval.</p>
      </div>
      <div class="form-body">
        <form action="/leave/apply-leave" method="POST">
          <label for="leaveType">Leave Type</label>
          <select name="leaveType" id="leaveType" required>
            <option value="" disabled selected>Select leave type</option>
            <option value="sick">Sick</option>
            <option value="vacation">Vacation</option>
            <option value="personal">Personal</option>
          </select>

          <label for="startDate">Start Date</label>
          <input type="date" name="startDate" id="startDate" required />

          <label for="endDate">End Date</label>
          <input type="date" name="endDate" id="endDate" required />

          <label for="reason">Reason</label>
          <textarea name="reason" id="reason" rows="4" placeholder="Explain your reason here..." required></textarea>

          <!-- Centered Buttons -->
          <div class="button-group">
            <button type="submit">Submit Leave Request</button>
            <button type="button" onclick="window.location.href='/dashboard'">Back</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer class="footer">
    &copy; 2024 WorkSpy. All Rights Reserved to 'M-A-L'.
  </footer>
</body>
</html>
